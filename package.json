{
  "name": "nav-button-creator",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "clipboard": "^1.7.1",
    "nav-button": "^1.0.12",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.0.17"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "push-dir --dir=build --branch=gh-pages"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
